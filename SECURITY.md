# 安全策略 (SECURITY)

## 报告漏洞

**请不要在公开 Issue 里描述可利用的漏洞。**

报告渠道 (按优先级):

1. **GitHub Security Advisories** (推荐): 仓库页 → Security → Advisories → New draft security advisory;
2. 没有 Advisory 权限时, 开一个 GitHub Issue **只写标题** (如 "安全问题: /api/fleet 越权"),
   细节通过 Issue 私下补充或请求维护者开启私密通道。

我们会在收到报告后尽快响应, 修复后于 CHANGELOG 中致谢 (可匿名)。

## 支持版本

| 版本 | 状态 | 是否提供安全修复 |
|---|---|---|
| 1.2.x | 维护中 | 是 |
| < 1.2 | 已停止 | 否 |

## 安全模型 (务必阅读)

v1.x 的威胁模型很朴素, 请在部署前知情:

| 项 | 现状 |
|---|---|
| TLS | **没有**。所有流量 (管理台 + WS 数据面) 明文 HTTP/WS |
| 鉴权/授权 | **没有**。任何能连上的人都可以读写串口字节、改配置、删除桥、退出程序 |
| 设计边界 | 仅限**可信网络**使用: 本机 (默认 127.0.0.1) 或受控内网 |

后果即是说: 把 SerialHub 暴露到公网或不可信局域网, 等于把串口设备的**完全控制权**
连同**明文数据流**一起交出去。**暴露公网风险自负** —— v1 明确不做 TLS/鉴权
(见 [docs/product/spec.md §6 非目标](docs/product/spec.md)), 请用 SSH 隧道等手段自己加壳,
或等待后续版本。

## 已知设计取舍

- `/ws` 数据面与 `/api` 控制面分离, 但**同等无鉴权**;
- `POST /api/shutdown` 可让整个程序退出, 无需确认;
- 慢客户端丢旧帧 (Lagged) 是有意的可用性取舍, 不是漏洞。
