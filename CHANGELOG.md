# SerialHub 更新日志

## v2.0.1 — 2026-09-15

### 修复 (全面测试反馈轮)
- 两段确认「确认态」悬停时文字同色不可读 (特异性问题) → armed+hover 4.75:1
- 回放状态消费: UI 跟随后端 replay 状态 + 循环回放可停止
- 浅色主题弱化文字 3.01:1 → 4.99:1 (AA)
- UI-0 禁词清理 / 删行焦点回归 / 回放失败话术按类别

## v2.0.0 — 2026-09-15

### 新增 (Sprint 13 · 批次 B)
- **录制与回放 (FR-19)**: 每桥抽屉新「录制」页签 —— 收发字节原样录成 JSONL 录像
  (每行 ts/dir/hex, 存 exe 旁 recordings/, `--recordings-dir` 可改), 按原始时序回放给串口
  (只回放 tx 行, 0.5~10 倍速 / 循环可调), 固件复现与测试注入用; 桥删除录像保留
- **旁路转发 (FR-20)**: 每桥可配转发目标 —— 串口收到的数据单向转发到 TCP 地址
  (只出不进防环路), 断线 3s 自动重连, 运行中改配热生效, 卡片显示连接状态
- **日志文件 (FR-21)**: `--log-file <路径>` 记录启动/状态迁移/错误/录制回放/转发事件,
  滚动 5MB×3 份; 手册附 Windows 服务化 (sc create) 说明
- **配置导入导出 (FR-22)**: 设置弹窗「导出配置 / 导入配置」—— 导出 fleet.json,
  合并/替换导入 (schema 校验, 失败不动现有表), 换机器迁移

### 契约
- fleet 桥对象 15→21 字段 (增 `autoOpen`/`running`/`recording`/`replay`/
  `forwardTcp`/`forwardConnected`); CLI 新增 `--recordings-dir`/`--log-file`

## v1.8.1 — 2026-09-14

### 修复
- 设置抽屉的「停止」与「✕ 关闭」统一为 28px 紧凑档成对按钮 (此前 34/28 混排突兀)
- 全站按钮归档复审: 46 颗可见按钮实测只存在 28/34 两档, 0 第三档、0 同排异档

## v1.8.0 — 2026-09-14

### 修复与改进 (用户实测反馈)
- **串口记忆修复**: 新建桥表单现在能记住上次的串口 (此前回填早于扫描列表填充被跳过)
- **波特率自由输入**: 自绘下拉不再拦截自由键入 —— 任意 [110,2000000] 数字直接生效,
  预设列表只是快捷方式
- **设置抽屉流程糖**: 抽屉内置与卡片同步的 停止/启动 钮; 桥运行中改配置保存时
  自动 停止→应用→启动 一次完成, 不再强迫"关抽屉→停止→重开"
- 顺带修复两个存量缺陷: 抽屉「保存」自 v1.3 起因携带后端必拒字段必失败;
  停止后立刻开抽屉读到旧相位

## v1.7.1 — 2026-09-14

### 修复 (用户实测反馈)
- **窗口记忆失效**: 根因 = 桥 CRUD 整份持久化时 [window] 段被抹掉 (旧 fleet.json
  升级首启无该段)。持久化改为读盘合并, 段不再丢失; 3 条回归测试钉住
- **新建桥表单参数记忆加固**: 字段级校验 (波特率仅认 [110,2000000] 数字、枚举只收
  选项集, 坏字段跳过不连坐), 任何情况下打开表单波特率不再为空
- **波特率下拉重做**: 原生 number spinner 与 datalist 箭头叠影 → 自绘下拉
  (幽灵 chevron, 输入过滤 + ↑↓回车键盘选择)
### 测试台
- conftest tasklist 输出解码容错 (中文 Windows 零匹配时输出 GBK 导致整批 error)

## v1.7.0 — 2026-09-14

### 新增 (批次 A · 体验补齐)
- **窗口大小/位置记忆**: 退出时保存, 重启逐像素恢复; 最大化状态单独记忆;
  超屏坐标自动夹紧回主屏 (换显示器/拔屏不怕丢窗口)
- **新建桥表单记住上次参数**: 串口/波特率/数据位/校验/停止位/流控自动回填
  (串口已拔则不回填), 端口仍自动递增; 编辑已有桥不受影响
- CI 增加 fmt/clippy 双门禁: 全仓格式化清偿 (~1075 行) + 19 条 clippy 警告
  真实修复, 代码风格从此有闸

### 修复
- win95 主题页脚对比度实测复核: 白字对青底 4.77:1 本就达标 (AA), 主题文件
  内误算的对比度数字全部纠正并用单测钉住

## v1.6.1 — 2026-09-14

### 改进: win95 主题正宗化 (用户实测反馈)
- 结构规则升级: 全站直角到底 (含弹窗/提示气泡/徽章)、按钮与面板凹凸立体边框
  (外凸/按下内陷)、页头藏蓝 #000080 标题栏、去柔和投影、Tahoma 小字号
- 说明: 浏览器字体反锯齿无法关闭, 真·像素字需内嵌位图字体 (约 +80KB, 候选项)
- 注意: themes/win95.css 落盘副本会遮蔽内置更新 —— 升级后若主题没变,
  删 themes/win95.css 重启即可 (新版本会重新落盘)

## v1.6.0 — 2026-09-14

### 新增: Windows 95 主题 (内置第四套)
- 经典复刻: 桌面青 #008080 底 + 银灰 #c0c0c0 面板 + 海军蓝 #000080 主色,
  **全站直角** (--ctl-radius:0), 终端黑底
- 配色致敬 Windows 95 与 98.css 项目; 徽章/终端对比度按 WCAG 自检
- 像素审计升级为主题感知 (期望圆角/高度随所切主题解析), win95 下
  160 控件 × 7 轮 0 违例
- 附带: 终端文字色令牌化 (--term-ink), 各主题可独立定义终端前景色

## v1.5.1 — 2026-09-14

### 改进
- 复制图标内嵌化: 网址/命令等容器为唯一表面, 复制图标为右缘幽灵图标
  (静默灰, hover 提亮), 消除"框上叠框"的双层视觉 (用户反馈)

## v1.5.0 — 2026-09-14

### 改进
- **复制交互图标化**: 全站复制动作改为图标钮 (28px 紧凑档), 成功后钮旁弹
  「已复制」微型气泡约 1.5s 自散 (全站共享一个气泡, 不堆叠), 替代原「复制」文字按钮
- **「打开面板」修复**: 桌面壳内改走新端点 `POST /api/open-console` —— 后端用系统
  默认浏览器打开管理台 (headless 脚本亦可触发), 不再受壳拦截 window.open 影响;
  浏览器页维持新标签, 端点失败才降级为复制网址
- **托盘右键修复**: 右键弹菜单不再误拉起主窗口致菜单即逝; 恢复窗口固定用左键,
  两键互不干扰
- 文档查缺补漏: 主题插件 / 单实例提示 / 新建桥端口预填 / release 无控制台取舍 /
  数据口双路径 / 打开面板等逐项进手册; 手册与 README 去重 (重复示例改链接),
  「控制台」统一为「管理台」

### 质量
- cargo test 83/83 · pytest 63/63 · 像素审计 155 控件 × 7 轮 0 违例 (新图标钮入 28px 档)
- 新增 Rust 单测 3 条 (托盘分键 ×2 / open-console 端点 ×1); 人工目视清单见
  docs/team/reports/qa-sprint9-plan.md

## v1.4.1 — 2026-09-14

### 兼容性
- 桥数据端点双路径受理: `ws://<桥网址>` (裸地址, 兼容 serial_bridge.py 时代习惯) 与
  `ws://<桥网址>/ws` (推荐写法) 等价; 用浏览器直接访问数据端口会得到明确的
  426 指引而非 404

## v1.4.0 — 2026-09-14

### 改进
- **重复打开不再报错**: 第二个实例探测到 SerialHub 已在运行时, 弹出友好提示
  (含管理台网址), 确定后自动在浏览器打开管理台; 被其他程序占用时仍如实报错
- **新建桥端口自动递增**: 表单预填下一个空闲端口 (跳过已用), 少一次输入
- **发布版无控制台窗口**: Windows release 构建为 GUI 子系统, 双击即用无黑窗;
  调试版保留控制台 (headless release 的 stdout 随之不可见, 属既定取舍)
- 附实用功能调研报告 (docs/team/reports/research-features.md): Top3 = 每桥数据
  录制回放 / TCP·MQTT 旁路转发 / Windows 服务化

## v1.3.0 — 2026-09-13

### 新增
- **管理台端口可在设置中修改**: 原地换绑 (串口与各桥数据面不受影响), 持久化
  恢复, 桌面窗口与浏览器页面自动跟随新地址 (FR-13)
- **页头「打开面板」按钮**: 一键在系统浏览器打开管理台 (桌面壳)
- **主题插件**: themes/ 文件夹放一个 .css 就是一套主题 (只覆盖设计令牌),
  管理台无刷新动态切换, 内置 浅色/深色/示例·奥利奥 (示范自制格式) (FR-14)
- **应用图标**: 全新「桥+串口母题」图标, exe/窗口/托盘三态/favicon 全生效 (FR-15)
- **最小接入示例**: examples/web-client.html (零依赖网页客户端) +
  examples/python-client.py, 配五步教程 (手册「程序接入」章)

### 质量
- cargo test 73/73 · pytest 52/52 · 像素审计扩至 7 轮 155 控件 0 违例
- CI 三平台矩阵全绿; tag 推送自动附加三平台预编译包

## v1.2.0 — 2026-09-12

### 新增: 自动重连每桥可选 (FR-12)
- 每座桥可独立开关「串口断开自动重连」(默认开): CLI --reconnect/--no-reconnect,
  建桥与设置均可改
- 关闭时串口断开直接「已停止」(不重试), 错误注明"(自动重连已关闭)";
  手动打开不受影响; 运行中改开关即时生效
- 契约: /api/status 13 字段、/api/fleet 桥对象 15 字段 (+autoReconnect)

### 改进: 控件尺度统一 (UI-1, 苹果式)
- 全站控件归入两档高: 标准 34px / 紧凑 28px, 圆角统一 8px, 控件字号 13px
- 「自动重连」开关重造为苹果式 44×28 轨道 switch (键盘可达)
- 像素级验收: 105 个可见控件 × 5 轮实测 0 违例 (tools/ui_pixel_audit.js)

## v1.1.0 — 2026-09-12

### 新增: 串口热拔插自动重连 (产品化)
- 串口意外断开 (拔线/设备重枚举) 时, 桥自动重连: 管理台明示
  「串口已断开，正在自动重连 (第 n 次)…」, 重试次数实时可见
- 恢复瞬间「串口已恢复」横幅 (约 3s), 不弹窗不打断
- 断开期间 WS 客户端连接保持, 恢复后数据自动续传 —— 客户端零动作
- 契约: /api/status 与 /api/fleet 桥对象新增 `retries` 字段
- 测试: FR-11 4 条 (占用→重连→释放→1.1s 自动恢复 / 客户端零动作续传);
  真机 COM8 拔插 10 次人工演练指南见 docs/team/reports/qa-sprint5-plan.md §4

## v1.0.0 — 2026-09-12

首个正式版: 串口 ⇄ WebSocket 桥接管理器。

### 核心能力
- **多桥管理台** (固定管理地址, 永远可达): 桥列表仪表盘, 每桥流程图
  (串口 ⇄ 桥 ⇄ 网址) + 四态徽章 + 收/发速率火花线 + 连接数/时长/错误
- **桥全生命周期**: 新建 / 启动 / 停止 / 改配 / 删除; fleet.json 持久化,
  进程重启自动恢复全部桥
- **原始字节双向管道**: WS 纯二进制帧, 多客户端广播, TX FIFO 仲裁;
  每桥数据端点 `ws://<网址>/ws`, 生命周期内端口稳定
- **串口全参数**: 110~2,000,000 波特, 7/8 数据位, N/E/O 校验, 1/2 停止位,
  none/rtscts/xonxoff 流控; CLI 与管理台完全对等
- **自动重开**: 串口掉线自动重试 (监督任务), 状态机 Closed/Opening/Open/Retry 全程可见
- **旁看**: 每桥「串口数据」抽屉只读查看原始流 (ASCII/HEX, 5000 行上限)
- **桌面客户端**: 原生窗口 + 托盘 (关窗即后台), --headless 纯 CLI 模式
- **退出三路**: 托盘菜单 / 管理台「退出程序」/ 任务管理器; 优雅停机有界兜底

### 质量
- cargo test 53/53 · pytest 36/36 (含 FR-10 多桥 7 条) · FR-10 7/7
- 性能基线: 双向 7.3/10.4 Mbps (阈值 0.9), RX→WS 延迟 p95 0.61ms
- 新手四问实测全过 (UI-0 自明性: 零文档上手)
- 多智能体团队 4 个 Sprint 开发→测试→使用反馈全回路 (编制 30 席, docs/team/org.md)

### 兼容性
- 旧单桥 CLI 参数完全兼容 (= 自动建一座桥并启动)
- 桥端点契约: /api/status 11 字段 (单桥模式), /api/fleet 桥对象 13 字段
